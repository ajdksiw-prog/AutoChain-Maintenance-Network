[project]
name = 'AutoChain-Maintenance-Network'
description = ''
authors = []
telemetry = true
cache_dir = '.\.cache'
requirements = []
[contracts.maintenance-record-system]
path = 'contracts/maintenance-record-system.clar'
clarity_version = 3
epoch = 'latest'

[contracts.vehicle-identity-registry]
path = 'contracts/vehicle-identity-registry.clar'
clarity_version = 3
epoch = '3.2'
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false

[repl.remote_data]
enabled = false
api_url = 'https://api.hiro.so'
